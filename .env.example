# Environment variables for RAMSspace application

# Backend API (if needed for future integration)
REACT_APP_API_URL=http://localhost:5000

# Build environment
NODE_ENV=production

# Feature flags
REACT_APP_ENABLE_ANALYTICS=false
REACT_APP_DEBUG_MODE=false
